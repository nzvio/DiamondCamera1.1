class Diamond {
	final double width;
    final String size;
    final String weight;
    final String filename;    
    bool active = false;

	Diamond({this.width, this.size, this.weight, this.filename, this.active});
}